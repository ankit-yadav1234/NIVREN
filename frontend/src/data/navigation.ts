/**
 * Navigation lives in config/navigation.ts (it is site configuration).
 * Re-exported here to match the data/ layer convention.
 */
export { mainNavigation, footerNavigation } from "@/config/navigation";
